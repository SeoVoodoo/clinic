import React from 'react';
import { styled } from 'styled-components';

export const Search = () => {
    return (
        <StyledSearch>
            
        </StyledSearch>
    );
};

const StyledSearch = styled.div`
    grid-area: search;
`